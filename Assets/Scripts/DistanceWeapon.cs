using UnityEngine;

[CreateAssetMenu(fileName = "DistanceWeapon", menuName = "Item Info/DistanceWeapon", order = 0)]
public class DistanceWeapon : ScriptableObject
{
    public float weaponDamage;
    public float critDamage;
    public float weight;
}
